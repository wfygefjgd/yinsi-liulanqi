name: yinsi_liulanqi
description: 隐私浏览器 - 极限无痕/核清 iOS 侧载
publish_to: "none"
version: 1.5.1+16

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_inappwebview: ^6.1.5
  path_provider: ^2.1.5
  provider: ^6.1.2
  share_plus: ^10.1.4
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
